// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command, form } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { formCoerce } from './form-utils.generated.js';
import { UpsertBolusCalculationRequestSchema } from '$lib/api/generated/schemas';
import { type UpsertBolusCalculationRequest } from '$api';

/** Lists records with pagination, optional date range, device, and source filtering. */
export const getAll = query(z.object({ from: z.coerce.date().optional(), to: z.coerce.date().optional(), limit: z.number().optional(), offset: z.number().optional(), sort: z.string().optional(), device: z.string().optional(), source: z.string().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.bolusCalculation.getAll(params?.from, params?.to, params?.limit, params?.offset, params?.sort, params?.device, params?.source);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { request, url } = getRequestEvent();
    const shareHost = request.headers.get('x-forwarded-host') ?? request.headers.get('host') ?? '';
    if (/^[^.]+\.share\./i.test(shareHost)) throw error(401, 'Unauthorized');
    throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in bolusCalculation.getAll:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to get all');
  }
});

/** Creates a new record and returns it with a `Location` header pointing to the created resource. */
export const create = form(formCoerce(UpsertBolusCalculationRequestSchema) as any, async (request) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.bolusCalculation.create(request as UpsertBolusCalculationRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in bolusCalculation.create:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to create');
  }
});

/** Retrieves a single record by its unique identifier. */
export const getById = query(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.bolusCalculation.getById(id);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { request, url } = getRequestEvent();
    const shareHost = request.headers.get('x-forwarded-host') ?? request.headers.get('host') ?? '';
    if (/^[^.]+\.share\./i.test(shareHost)) throw error(401, 'Unauthorized');
    throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in bolusCalculation.getById:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to get by id');
  }
});

/** Updates an existing record by ID and returns the updated record. */
export const update = form(formCoerce(z.object({ id: z.string(), request: UpsertBolusCalculationRequestSchema })) as any, async ({ id, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.bolusCalculation.update(id, request as UpsertBolusCalculationRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in bolusCalculation.update:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to update');
  }
});

/** Deletes a record by ID. */
export const remove = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.bolusCalculation.delete(id);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in bolusCalculation.delete:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to remove');
  }
});

/** Lists soft-deleted records available for restoration, ordered by deletion date (newest first). */
export const listDeleted = query(z.object({ limit: z.number().optional(), offset: z.number().optional() }).optional(), async (params) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.bolusCalculation.listDeleted(params?.limit, params?.offset);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { request, url } = getRequestEvent();
    const shareHost = request.headers.get('x-forwarded-host') ?? request.headers.get('host') ?? '';
    if (/^[^.]+\.share\./i.test(shareHost)) throw error(401, 'Unauthorized');
    throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in bolusCalculation.listDeleted:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to list deleted');
  }
});

/** Restores a soft-deleted record by ID. */
export const restore = command(z.string(), async (id) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.bolusCalculation.restore(id);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in bolusCalculation.restore:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to restore');
  }
});

/** Restores multiple soft-deleted records by their IDs. */
export const bulkRestore = command(async () => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.bolusCalculation.bulkRestore();
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in bolusCalculation.bulkRestore:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to bulk restore');
  }
});
