// AUTO-GENERATED - DO NOT EDIT
// Generated by openapi-remote-codegen
// Source: openapi.json

import { getRequestEvent, query, command } from '$app/server';
import { error, redirect } from '@sveltejs/kit';
import { z } from 'zod';
import { AdminResetCursorsRequestSchema } from '$lib/api/generated/schemas';
import { type AdminResetCursorsRequest } from '$api';

/** List the connectors a target tenant has configured, with their last-sync and health state. */
export const getTenantConnectors = query(z.string(), async (tenantId) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.connectorAdmin.getTenantConnectors(tenantId);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { request, url } = getRequestEvent();
    const shareHost = request.headers.get('x-forwarded-host') ?? request.headers.get('host') ?? '';
    if (/^[^.]+\.share\./i.test(shareHost)) throw error(401, 'Unauthorized');
    throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in connectorAdmin.getTenantConnectors:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to get tenant connectors');
  }
});

/** Enqueue a background reset of the sync cursor for every connector configured on a target
tenant, forcing a re-pull of history. Use after fixing a connector bug to push corrected data
to an affected tenant. */
export const resetTenantCursors = command(z.object({ tenantId: z.string(), request: AdminResetCursorsRequestSchema }), async ({ tenantId, request }) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    const result = await apiClient.connectorAdmin.resetTenantCursors(tenantId, request as AdminResetCursorsRequest);
    return result;
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in connectorAdmin.resetTenantCursors:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to reset tenant cursors');
  }
});

/** Get the progress of a connector cursor reset job, including per-connector outcomes as they land. */
export const getResetJobStatus = query(z.string(), async (jobId) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    return await apiClient.connectorAdmin.getResetJobStatus(jobId);
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { const { request, url } = getRequestEvent();
    const shareHost = request.headers.get('x-forwarded-host') ?? request.headers.get('host') ?? '';
    if (/^[^.]+\.share\./i.test(shareHost)) throw error(401, 'Unauthorized');
    throw redirect(302, `/auth/login?returnUrl=${encodeURIComponent(url.pathname + url.search)}`); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in connectorAdmin.getResetJobStatus:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to get reset job status');
  }
});

/** Request cancellation of a running connector cursor reset job. Connectors already re-pulled
keep their committed data; the fan-out simply stops before the next connector. */
export const cancelResetJob = command(z.string(), async (jobId) => {
  const apiClient = getRequestEvent().locals.apiClient;
  try {
    await apiClient.connectorAdmin.cancelResetJob(jobId);
    await Promise.all([
      getResetJobStatus(jobId).refresh()
    ]);
    return { success: true };
  } catch (err) {
    const status = (err as any)?.status;
    if (status === 401) { throw error(401, 'Unauthorized'); }
    if (status === 403) throw error(403, (err as any)?.message ?? (err as any)?.detail ?? 'Forbidden');
    console.error('Error in connectorAdmin.cancelResetJob:', err);
    const e = err as any;
    const body = e?.body ?? e?.response;
    const errors = body?.errors ?? e?.errors;
    const flat = errors ? Object.entries(errors).map(([, v]: [string, any]) => Array.isArray(v) ? v.join(', ') : v).join('; ') : undefined;
    const message = flat ?? body?.message ?? body?.title ?? body?.detail ?? e?.message ?? e?.title ?? e?.detail;
    if (status === 400 || status === 409) throw error(status, message ?? 'Request rejected');
    throw error(500, message ?? 'Failed to cancel reset job');
  }
});
